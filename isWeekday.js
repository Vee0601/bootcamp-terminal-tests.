export default function isWeekday (Weekday){
    let Week_day = ['Saturday', 'Sunday'];
  if(Weekday==='Saturday' || Weekday==='Sunday'){ 
      return false;
      }
      else {
      return true;
      }
  }